<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Zi &amp; Victor | Our Story</title>

  <style>
    :root {
      --cream: #fff8f1;
      --paper: #fffdf9;
      --ink: #332b2a;
      --muted: #806d68;
      --rose: #c76868;
      --rose-dark: #984d55;
      --peach: #f6d6c4;
      --line: #ecdcd4;
      --shadow: 0 18px 50px rgba(126, 75, 68, 0.12);
    }

    * { box-sizing: border-box; }

    html { scroll-behavior: smooth; }

    body {
      margin: 0;
      color: var(--ink);
      background: var(--cream);
      font-family: Georgia, "Times New Roman", serif;
      line-height: 1.7;
    }

    body::before {
      content: "";
      position: fixed;
      inset: 0;
      z-index: -1;
      opacity: .3;
      pointer-events: none;
      background-image: radial-gradient(#d7aaa0 0.7px, transparent 0.7px);
      background-size: 22px 22px;
    }

    .nav {
      position: sticky;
      top: 0;
      z-index: 10;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 18px 7%;
      background: rgba(255, 248, 241, .88);
      border-bottom: 1px solid rgba(236, 220, 212, .8);
      backdrop-filter: blur(12px);
    }

    .brand {
      color: var(--rose-dark);
      font-size: 1.25rem;
      font-weight: bold;
      text-decoration: none;
    }

    .links { display: flex; gap: 25px; }

    .links a {
      color: var(--muted);
      font-size: .85rem;
      text-decoration: none;
      transition: color .2s ease;
    }

    .links a:hover { color: var(--rose); }

    .hero {
      min-height: 86vh;
      display: grid;
      place-items: center;
      padding: 80px 7%;
      text-align: center;
      background: linear-gradient(135deg, rgba(255,248,241,.7), rgba(246,214,196,.65));
    }

    .hero-content { max-width: 780px; }

    .eyebrow {
      margin: 0 0 18px;
      color: var(--rose);
      font: bold .78rem/1.2 Arial, sans-serif;
      letter-spacing: .22em;
      text-transform: uppercase;
    }

    h1 {
      margin: 0;
      color: var(--rose-dark);
      font-size: clamp(4rem, 13vw, 9rem);
      font-weight: normal;
      line-height: .95;
    }

    .amp {
      color: var(--rose);
      font-style: italic;
      font-size: .6em;
    }

    .hero p {
      max-width: 520px;
      margin: 28px auto 34px;
      color: var(--muted);
      font-size: 1.18rem;
    }

    .button {
      display: inline-block;
      padding: 13px 25px;
      border: 1px solid var(--rose);
      border-radius: 30px;
      color: white;
      background: var(--rose);
      font: bold .78rem Arial, sans-serif;
      letter-spacing: .08em;
      text-decoration: none;
      text-transform: uppercase;
      box-shadow: 0 9px 20px rgba(199, 104, 104, .2);
      transition: transform .2s ease, background .2s ease;
    }

    .button:hover {
      background: var(--rose-dark);
      transform: translateY(-2px);
    }

    section {
      max-width: 1060px;
      margin: auto;
      padding: 90px 7%;
    }

    .section-heading { margin-bottom: 35px; }

    h2 {
      margin: 5px 0 10px;
      color: var(--rose-dark);
      font-size: clamp(2.3rem, 5vw, 3.8rem);
      font-weight: normal;
      line-height: 1.05;
    }

    .section-heading p { color: var(--muted); }

    .gallery {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      grid-auto-rows: 155px;
      gap: 14px;
    }

    .memory {
      position: relative;
      display: flex;
      align-items: end;
      overflow: hidden;
      padding: 20px;
      border-radius: 8px;
      color: white;
      background: linear-gradient(145deg, #eaa99b, #9e5b68);
      box-shadow: var(--shadow);
    }

    .memory::before {
      content: "♥";
      position: absolute;
      top: 18px;
      right: 20px;
      color: rgba(255,255,255,.5);
      font-size: 2rem;
    }

    .memory:nth-child(2) { background: linear-gradient(145deg, #eecb9d, #b87865); }
    .memory:nth-child(3) { background: linear-gradient(145deg, #adbdc4, #64757f); }
    .memory:nth-child(4) { background: linear-gradient(145deg, #d7a4b5, #78516d); }
    .memory:nth-child(5) { background: linear-gradient(145deg, #f0c5a5, #c77972); }
    .memory:nth-child(6) { background: linear-gradient(145deg, #b6c4a9, #73816d); }

    .memory.wide { grid-column: span 2; }
    .memory.tall { grid-row: span 2; }

    .memory strong {
      position: relative;
      font-size: 1.12rem;
      font-weight: normal;
    }

    .timeline {
      position: relative;
      margin-top: 45px;
      padding-left: 30px;
      border-left: 1px solid var(--rose);
    }

    .event { position: relative; margin: 0 0 42px; }
    .event:last-child { margin-bottom: 0; }

    .event::before {
      content: "";
      position: absolute;
      left: -37px;
      top: 6px;
      width: 13px;
      height: 13px;
      border: 4px solid var(--cream);
      border-radius: 50%;
      background: var(--rose);
      box-shadow: 0 0 0 1px var(--rose);
    }

    .date {
      color: var(--rose);
      font: bold .75rem Arial, sans-serif;
      letter-spacing: .15em;
      text-transform: uppercase;
    }

    .event h3 { margin: 6px 0 5px; font-size: 1.5rem; font-weight: normal; }
    .event p { margin: 0; color: var(--muted); }

    .letter-wrap { text-align: center; }

    .letter {
      max-width: 700px;
      margin: 35px auto 0;
      padding: clamp(30px, 7vw, 70px);
      text-align: left;
      background: var(--paper);
      border: 1px solid var(--line);
      box-shadow: var(--shadow);
      transform: rotate(-.5deg);
    }

    .letter::before {
      content: "♥";
      display: block;
      margin-bottom: 18px;
      color: var(--rose);
      font-size: 2rem;
      text-align: center;
    }

    .letter p { margin: 0 0 20px; color: #5d4c48; font-size: 1.12rem; }
    .signature { color: var(--rose-dark) !important; font-style: italic; }

    footer {
      padding: 35px 7%;
      color: var(--muted);
      border-top: 1px solid var(--line);
      text-align: center;
      font-size: .9rem;
    }

    @media (max-width: 650px) {
      .nav { padding: 15px 5%; }
      .links { gap: 12px; }
      .links a { font-size: .72rem; }
      section { padding: 65px 5%; }
      .gallery { grid-template-columns: repeat(2, 1fr); grid-auto-rows: 140px; }
      .memory.wide { grid-column: span 2; }
      .memory.tall { grid-row: span 2; }
      .hero { min-height: 78vh; padding: 60px 5%; }
    }
  </style>
</head>
<body>
  <nav class="nav" aria-label="Main navigation">
    <a class="brand" href="#top">Z &amp; V</a>
    <div class="links">
      <a href="#memories">Memories</a>
      <a href="#story">Our story</a>
      <a href="#letter">Letter</a>
    </div>
  </nav>

  <header class="hero" id="top">
    <div class="hero-content">
      <p class="eyebrow">A little corner of the internet</p>
      <h1>Zi <span class="amp">&amp;</span> Victor</h1>
      <p>Two hearts, one beautiful story, and countless little moments worth remembering.</p>
      <a class="button" href="#memories">Explore our story</a>
    </div>
  </header>

  <main>
    <section id="memories">
      <div class="section-heading">
        <p class="eyebrow">The best kind of memories</p>
        <h2>Little moments,<br>big feelings.</h2>
        <p>A collection of the places, days, and simple moments that make us smile.</p>
      </div>

      <div class="gallery" aria-label="A collection of memories">
        <div class="memory tall"><strong>Our favorite adventures</strong></div>
        <div class="memory wide"><strong>Laughing until it hurts</strong></div>
        <div class="memory"><strong>Golden-hour walks</strong></div>
        <div class="memory"><strong>Just us</strong></div>
        <div class="memory wide"><strong>Every day is better together</strong></div>
        <div class="memory"><strong>Always my person</strong></div>
      </div>
    </section>

    <section id="story">
      <div class="section-heading">
        <p class="eyebrow">How it all began</p>
        <h2>Our story</h2>
        <p>The moments that brought us here and all the ones still waiting ahead.</p>
      </div>

      <div class="timeline">
        <article class="event">
          <span class="date">The beginning</span>
          <h3>When our paths crossed</h3>
          <p>Somehow, in a world full of people, we found each other. That was the start of something special.</p>
        </article>
        <article class="event">
          <span class="date">A little later</span>
          <h3>More than a coincidence</h3>
          <p>Every conversation became a favorite, and every ordinary day started to feel a little brighter.</p>
        </article>
        <article class="event">
          <span class="date">Right now</span>
          <h3>Still choosing you</h3>
          <p>We are writing the best part of our story one laugh, one adventure, and one memory at a time.</p>
        </article>
      </div>
    </section>

    <section class="letter-wrap" id="letter">
      <p class="eyebrow">A note from the heart</p>
      <h2>For you, always.</h2>
      <div class="letter">
        <p>Dear Victor,</p>
        <p>Thank you for making the ordinary feel magical. I love the way we can turn the smallest moments into memories I want to keep forever.</p>
        <p>Whatever comes next, I am grateful that I get to experience it with you. You are my favorite place to be, my safest feeling, and my greatest adventure.</p>
        <p class="signature">With all my love,<br>Zi ♥</p>
      </div>
    </section>
  </main>

  <footer>
    Made with love for Zi &amp; Victor · Always and forever
  </footer>
</body>
</html>
